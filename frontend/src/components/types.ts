export interface mazeGridFromServer {
  row: number, col: number, northEdge: boolean, southEdge: boolean, westEdge: boolean, eastEdge: boolean
}

export interface path {
  row : number, col : number
}